const cars = ["Honda", "Toyota", "Subaru"]
let lastcars=cars.pop()
cars.pop();
console.log(cars);


const matrix = [
    [1, 2, 3],
    [4, 5, 6],
    [7, 8, 9]
];

console.log([1],[2]);const arr = ["t", "e", "s", "t"];

console.log( arr.slice(1,3)); //e,s copy from 1 to 3


// Splice, slice, find, filter. Start on the ones in Module3LabScript. Change.